(function(){const SETTINGS_KEY="gc_site_settings";const SETTINGS_ROW="public_site";const SUBMISSIONS_KEY="gc_public_submissions";const SUBMISSIONS_TABLE="public_submissions";const roles=["worker","restaurant","supplier"];let allowBackgrounds=false;const roleName={worker:"работника",restaurant:"заведения",supplier:"поставщика",};const normalizeRole=(role)=>(roles.includes(role)?role:"worker");function getPath(source,path){return path.split(".").reduce((value,key)=>value?.[key],source)}function mergeDeep(base,extra){if(!extra || typeof extra !=="object")return base ||{};const result={...(base ||{})};Object.entries(extra).forEach(([key,value])=>{if(value && typeof value==="object" && !Array.isArray(value)){result[key]=mergeDeep(result[key],value)}else if(value !==undefined && value !==null && value !==""){result[key]=value}});return result}function readJson(key,fallback){try{return JSON.parse(localStorage.getItem(key)|| "")|| fallback}catch{return fallback}}function writeJson(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch{}}function applySettings(settings){if(!settings || typeof settings !=="object")return;document.querySelectorAll("[data-site-setting]").forEach((node)=>{const value=getPath(settings,node.dataset.siteSetting);if(typeof value==="string")node.textContent=value});document.querySelectorAll("[data-site-image]").forEach((image)=>{const value=getPath(settings,image.dataset.siteImage);setSafeLocalImage(image,value)});if(allowBackgrounds){document.querySelectorAll("[data-site-bg]").forEach((node)=>{const value=getPath(settings,node.dataset.siteBg);setSafeLocalBackground(node,value)})}document.querySelectorAll("[data-site-logo]").forEach((image)=>{setSafeLocalImage(image,settings.logo)})}function safeLocalAsset(src){const value=String(src || "").trim();if(!value)return "";const lower=value.toLowerCase();if(lower.startsWith("javascript:")|| lower.startsWith("data:")||(lower.includes("githubusercontent")&& lower.includes("raw"))){return ""}if(lower.startsWith("assets/"))return value;if(lower.startsWith("/assets/"))return value.slice(1);try{const url=new URL(value,window.location.href);return url.origin===window.location.origin && url.pathname.startsWith("/assets/")?`${url.pathname}${url.search}`:""}catch{return ""}}function setSafeLocalImage(image,src){if(!image || typeof src !=="string")return;const nextSrc=safeLocalAsset(src);if(!nextSrc || image.getAttribute("src")===nextSrc)return;const fallback=image.getAttribute("src")|| nextSrc;image.onerror=()=>{if(fallback && image.getAttribute("src")!==fallback)image.src=fallback};image.src=nextSrc}function setSafeLocalBackground(node,src){if(!node || typeof src !=="string")return;const nextSrc=safeLocalAsset(src);if(!nextSrc)return;const escaped=nextSrc.replace(/["\\]/g,"\\$&");node.style.setProperty("--hero-image",`url("${escaped}")`)}function applyDefaultBackgrounds(){allowBackgrounds=true;document.querySelectorAll("[data-bg-default]").forEach((node)=>{setSafeLocalBackground(node,node.dataset.bgDefault)});applySettings(readJson(SETTINGS_KEY,{}))}function scheduleBackgrounds(){const run=()=>setTimeout(applyDefaultBackgrounds,0);if(document.readyState==="complete"){run()}else{window.addEventListener("load",run,{once:true})}}async function refreshSettings(){const client=window.supabaseClient;if(!client)return;try{const{data,error}=await client .from("site_settings").select("settings").eq("id",SETTINGS_ROW).maybeSingle();if(error || !data?.settings)return;const next=mergeDeep(readJson(SETTINGS_KEY,{}),data.settings);writeJson(SETTINGS_KEY,next);applySettings(next)}catch{}}async function getSupabaseRestConfig(){if(window.__gcSupabaseRestConfig)return window.__gcSupabaseRestConfig;if(window.supabaseClient?.supabaseUrl && window.supabaseClient?.supabaseKey){window.__gcSupabaseRestConfig={url:window.supabaseClient.supabaseUrl,key:window.supabaseClient.supabaseKey,};return window.__gcSupabaseRestConfig}const response=await fetch("/supabase.js?v=1002",{cache:"force-cache"});if(!response.ok)throw new Error("supabase.js не загрузился");const source=await response.text();const url=source.match(/SUPABASE_URL\s*=\s*['"]([^'"]+)['"]/i)?.[1];const key=source.match(/SUPABASE_ANON_KEY\s*=\s*['"]([^'"]+)['"]/i)?.[1];if(!url || !key)throw new Error("Supabase config не найден");window.__gcSupabaseRestConfig={url,key};return window.__gcSupabaseRestConfig}function readSubmissions(){return readJson(SUBMISSIONS_KEY,[])}function saveLocalSubmission(row){const rows=readSubmissions();rows.unshift(row);writeJson(SUBMISSIONS_KEY,rows.slice(0,100))}function createSubmissionId(){if(window.crypto && typeof window.crypto.randomUUID==="function"){return window.crypto.randomUUID()}return "10000000-1000-4000-8000-100000000000".replace(/[018]/g,(value)=>(Number(value)^(Math.random()*16>>Number(value)/4)).toString(16),)}function makeSubmission(type,data){const title=type==="callback"?`Заказ звонка:${data.name || data.phone || "контакт"}`:type==="feedback"?`Обратная связь:${data.name || data.phone || "контакт"}`:type==="telegram_bot"?`Telegram-уведомления:${data.telegram || data.name || "контакт"}`:data.name || data.company || data.role || data.product || "Заявка";return{id:createSubmissionId(),type,title,phone:data.phone || "",telegram:data.telegram || "",city:data.city || "",data,source:"site",created_at:new Date().toISOString(),}}async function saveRemoteSubmission(row){const client=window.supabaseClient;if(client){const{error}=await client.from(SUBMISSIONS_TABLE).insert(row);if(error)throw error;return true}const{url,key}=await getSupabaseRestConfig();const response=await fetch(`${url}/rest/v1/${SUBMISSIONS_TABLE}`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:"return=minimal",},body:JSON.stringify(row),});if(!response.ok)throw new Error(`public_submissions ${response.status}`);return true}function initPublicForms(){document.querySelectorAll("form[data-form-type]").forEach((form)=>{form.addEventListener("submit",async(event)=>{event.preventDefault();const button=form.querySelector('button[type="submit"],input[type="submit"]');const box=form.querySelector(".success");if(button)button.disabled=true;const row=makeSubmission(form.dataset.formType,Object.fromEntries(new FormData(form).entries()),);saveLocalSubmission(row);try{await saveRemoteSubmission(row);if(box){box.textContent=form.dataset.formType==="callback"?"Заявка на звонок отправлена. Мы свяжемся с вами по указанному телефону.":form.dataset.formType==="feedback"?"Обращение отправлено. Мы ответим по указанным контактам.":form.dataset.formType==="telegram_bot"?"Заявка на Telegram-уведомления отправлена. Мы свяжемся для подключения.":"Заявка отправлена. Дальше работайте с ней в личном кабинете."}}catch{if(box)box.textContent="Заявка сохранена локально. Если интернет или база недоступны,попробуйте отправить еще раз позже."}finally{if(box)box.style.display="block";form.reset();if(button)button.disabled=false}})})}function initAuthPage(){const methodInput=document.getElementById("authMethod");const nameInput=document.getElementById("displayName");const cityInput=document.getElementById("city");const emailInput=document.getElementById("email");const phoneInput=document.getElementById("phone");const emailField=document.getElementById("emailField");const phoneField=document.getElementById("phoneField");const nameField=document.getElementById("nameField");const cityField=document.getElementById("cityField");const passwordInput=document.getElementById("password");const roleInput=document.getElementById("role");const roleField=document.getElementById("roleField");const message=document.getElementById("authMessage");const registerBtn=document.getElementById("registerBtn");const loginBtn=document.getElementById("loginBtn");const showLoginBtn=document.getElementById("showLoginBtn");const showRegisterBtn=document.getElementById("showRegisterBtn");const formTitle=document.getElementById("authFormTitle");const modeHint=document.getElementById("authModeHint");const cabinetShortcut=document.getElementById("cabinetShortcut");if(!methodInput || !emailInput || !phoneInput || !passwordInput || !roleInput || !message)return;const params=new URLSearchParams(window.location.search);let mode=params.get("mode")==="login"?"login":"signup";let roleWasExplicit=roles.includes(params.get("role"));roleInput.value=normalizeRole(params.get("role"));methodInput.value=params.get("method")==="phone"?"phone":"email";const profileCabinetUrl=(profile)=>{const role=profile?.role || roleInput.value;return `/cabinet/?role=${encodeURIComponent(role)}`};function cleanPhone(value){let digits=String(value || "").replace(/\D/g,"");if(digits.length===11 && digits.startsWith("8"))digits=`7${digits.slice(1)}`;if(digits.length===10)digits=`7${digits}`;return digits?`+${digits}`:""}function emailValue(){return emailInput.value.trim().toLowerCase()}function phoneValue(){return cleanPhone(phoneInput.value)}function roleFromUser(user){return roles.find((role)=>role===user?.app_metadata?.role || role===user?.user_metadata?.role)|| ""}function setBusy(isBusy,text){[registerBtn,loginBtn,showLoginBtn,showRegisterBtn,methodInput,roleInput].forEach((control)=>{if(control)control.disabled=isBusy});if(text)message.textContent=text}function updateHistory(){const next=new URLSearchParams(window.location.search);next.set("mode",mode);next.set("role",roleInput.value);next.set("method",methodInput.value);window.history.replaceState(null,"",`/auth/?${next}`)}function setContactVisibility(updateUrl=true){const byPhone=methodInput.value==="phone";const loginMode=mode==="login";if(emailField)emailField.hidden=loginMode && byPhone;if(phoneField)phoneField.hidden=loginMode && !byPhone;if(nameField)nameField.hidden=loginMode;if(cityField)cityField.hidden=loginMode;emailInput.required=loginMode?!byPhone:methodInput.value==="email";phoneInput.required=loginMode?byPhone:methodInput.value==="phone";emailInput.placeholder=byPhone?"Можно указать дополнительно":"example@mail.ru";phoneInput.placeholder=byPhone?"+7 910 000-00-00":"Можно указать дополнительно";if(updateUrl)updateHistory()}function setMode(nextMode,updateUrl=true){mode=nextMode==="login"?"login":"signup";if(formTitle)formTitle.textContent=mode==="login"?"Вход":"Регистрация";if(roleField)roleField.hidden=mode==="login";if(registerBtn)registerBtn.hidden=mode !=="signup";if(loginBtn)loginBtn.hidden=mode !=="login";passwordInput.autocomplete=mode==="login"?"current-password":"new-password";showLoginBtn?.classList.toggle("is-active",mode==="login");showRegisterBtn?.classList.toggle("is-active",mode==="signup");if(modeHint){modeHint.textContent=mode==="login"?"Введите email или телефон и пароль. Роль подтянется из вашего профиля.":`Будет создан кабинет ${roleName[roleInput.value]}. Данные сохранятся в базе и админке.`}if(cabinetShortcut)cabinetShortcut.href=profileCabinetUrl({role:roleInput.value});setContactVisibility(false);if(updateUrl)updateHistory()}async function upsertAdminAccount(user,role,payload={}){if(!user || !window.supabaseClient)return;await window.supabaseClient.from("admin_user_accounts").upsert({user_id:user.id,role:normalizeRole(role),email:user.email || payload.email || emailValue()|| null,phone:user.phone || payload.phone || phoneValue()|| null,name:payload.name || nameInput?.value.trim()|| user.email || user.phone || "Пользователь",city:payload.city || cityInput?.value.trim()|| null,auth_provider:payload.auth_provider || methodInput.value,status:"active",source:"auth_page",raw_meta:user.user_metadata ||{},updated_at:new Date().toISOString(),},{onConflict:"user_id"})}async function ensureProfile(user,role){if(!user)return null;const profileRole=roleFromUser(user)||(roles.includes(role)?role:"");const authEmail=user.email || emailValue();const authPhone=user.phone || phoneValue();const authProvider=authPhone && !authEmail?"phone":"email";const profileName=nameInput?.value.trim()|| authEmail || authPhone || "Пользователь";const profileCity=cityInput?.value.trim()|| "";const{data:existing,error:readError}=await window.supabaseClient .from("profiles").select("*").eq("id",user.id).maybeSingle();if(readError)throw readError;if(existing){const patch={email:existing.email || authEmail || null,phone:existing.phone || authPhone || null,auth_provider:existing.auth_provider || authProvider,updated_at:new Date().toISOString(),};const{data:updated}=await window.supabaseClient .from("profiles").update(patch).eq("id",user.id).select("*").maybeSingle();await upsertAdminAccount(user,existing.role,{...existing,...patch});return updated || existing}if(!profileRole){throw new Error("Профиль не найден. Откройте регистрацию,выберите роль и создайте профиль.")}const payload={id:user.id,role:profileRole,name:profileName,city:profileCity || null,email:authEmail || null,phone:authPhone || null,auth_provider:authProvider,status:"active",updated_at:new Date().toISOString(),};const{error}=await window.supabaseClient.from("profiles").upsert(payload,{onConflict:"id"});if(error)throw error;await upsertAdminAccount(user,profileRole,payload);return payload}async function refreshSession(){const client=window.supabaseClient;if(!client || !cabinetShortcut)return;const{data}=await client.auth.getSession();if(data?.session?.user){try{const sessionProfile=await ensureProfile(data.session.user,roleWasExplicit?roleInput.value:"");cabinetShortcut.hidden=false;cabinetShortcut.href=profileCabinetUrl(sessionProfile);message.textContent="Вы уже вошли. Можно открыть кабинет."}catch(error){cabinetShortcut.hidden=true;message.textContent=error.message}}}registerBtn?.addEventListener("click",async()=>{const client=window.supabaseClient;const method=methodInput.value==="phone"?"phone":"email";const email=emailValue();const phone=phoneValue();const password=passwordInput.value;const role=normalizeRole(roleInput.value);const name=nameInput?.value.trim()|| "";const city=cityInput?.value.trim()|| "";if(!client)return(message.textContent="Supabase не загрузился. Обновите страницу.");if(method==="email" && !email)return(message.textContent="Введите email.");if(method==="phone" && !phone)return(message.textContent="Введите телефон в формате+7...");if(!password)return(message.textContent="Введите пароль.");if(password.length<6)return(message.textContent="Пароль должен быть минимум 6 символов.");setBusy(true,"Создаем аккаунт...");const options={data:{role,name,city,email,phone,auth_provider:method}};const signUpPayload=method==="phone"?{phone,password,options}:{email,password,options};const{data,error}=await client.auth.signUp(signUpPayload);if(error){setBusy(false);return(message.textContent=`Ошибка регистрации:${error.message}`)}if(data?.session?.user){try{const profile=await ensureProfile(data.session.user,role);window.location.href=profileCabinetUrl(profile);return}catch(profileError){setBusy(false);return(message.textContent=`Аккаунт создан,но профиль не сохранен:${profileError.message}`)}}setBusy(false);message.textContent=method==="phone"?"Аккаунт создан. Если Supabase просит SMS-код,подтвердите телефон и войдите.":"Аккаунт создан. Если Supabase просит подтверждение email,подтвердите почту и войдите.";setMode("login")});loginBtn?.addEventListener("click",async()=>{const client=window.supabaseClient;const method=methodInput.value==="phone"?"phone":"email";const email=emailValue();const phone=phoneValue();const password=passwordInput.value;if(!client)return(message.textContent="Supabase не загрузился. Обновите страницу.");if(method==="email" && !email)return(message.textContent="Введите email.");if(method==="phone" && !phone)return(message.textContent="Введите телефон.");if(!password)return(message.textContent="Введите пароль.");setBusy(true,"Входим...");const credentials=method==="phone"?{phone,password}:{email,password};const{data,error}=await client.auth.signInWithPassword(credentials);if(error){setBusy(false);return(message.textContent=`Ошибка входа:${error.message}`)}try{const profile=await ensureProfile(data.user,roleWasExplicit?roleInput.value:"");window.location.href=profileCabinetUrl(profile)}catch(profileError){setBusy(false);message.textContent=`Вход выполнен,но профиль не проверен:${profileError.message}`}});showLoginBtn?.addEventListener("click",()=>setMode("login"));showRegisterBtn?.addEventListener("click",()=>setMode("signup"));roleInput.addEventListener("change",()=>{roleWasExplicit=true;setMode(mode)});methodInput.addEventListener("change",()=>setContactVisibility(true));passwordInput.addEventListener("keydown",(event)=>{if(event.key !=="Enter")return;event.preventDefault();(mode==="login"?loginBtn:registerBtn)?.click()});setMode(mode,false);setContactVisibility(false);refreshSession()}applySettings(readJson(SETTINGS_KEY,{}));scheduleBackgrounds();initPublicForms();initAuthPage();window.addEventListener("load",()=>setTimeout(refreshSettings,400),{once:true})})();